{
  "name": "arling-asistent-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for ARLing Asistent: feed ingestion, embeddings, grounded chat, tenant onboarding, daily cron.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:schema": "wrangler d1 execute asistent --file=schema.sql --remote",
    "d1:schema:local": "wrangler d1 execute asistent --file=schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "license": "UNLICENSED",
  "author": "ARLing s. r. o. <andrej@arling.sk>"
}
